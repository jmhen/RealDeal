<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="capstone.realdeal">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".DashboardActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".payment.PaymentActivity"/>
        <activity android:name=".property.PropertiesManagement"/>
        <activity android:name=".DocumentsManagement"/>
        <activity android:name=".payment.PayActivity"/>
        <activity android:name=".payment.RequestActivity"/>
        <activity android:name=".payment.ReceiptActivity"/>
        <activity android:name=".property.PropertyDetails"/>
        <activity android:name=".property.RoomDetails"/>
        <activity android:name=".ProfileActivity"/>
        <activity android:name=".Setting"/>

    </application>

</manifest>